# Course api

## Launching

```PS
npm run start
```

## Endpoints

- getList (search request) (+)
- getProduct (id) (+)
- createProduct (+)
- deleteProduct (id)
- updateProduct

- placeOrder
- auth

ProductEntity {
id: string;
name: string;
price: number;
category: string;
amount: number;
imgUrl: string;
}
